HttpClient provides a modern alternative to HttpURLConnection.It's an efficient, and feature-rich API for HTTP interations . 
It's an efficient, and feature-rich API for HTTP interactions.
Oracle has been prioritizing development efforts on HttpClient,indicating a clear preference for its usage in new projects.
Leading framezorks and libraries in the java ecosystem, such as Spring, are increasingly adopting the JDK's HttpClient